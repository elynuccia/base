{% extends 'base.html.twig' %}
{% block title %}Register your school!{% endblock title %}

{% block stylesheets %}
    {{ parent() }}
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <!-- Bootstrap core CSS -->
    <link rel="stylesheet" href={{ asset('css/bootstrap.css') }} >
    <!-- Material Design Bootstrap -->
    <link rel="stylesheet" href={{ asset('css/mdb.css') }} >
    <!-- Your custom styles (optional) -->


    <link rel="stylesheet" href={{ asset('css/bootstrap.min.css') }} >
    <link rel="stylesheet" href={{ asset('css/mdb.min.css') }} >
    <link rel="stylesheet" href={{ asset('css/style.css') }} >

{% endblock %}


{% block body %}
   <h1>Welcome to the BASE Application</h1>
    <div class="container">
        <div class="row">
            <div class="col-sm">
            </div>
            <div class="col-sm">
                <h2>... Description ...</h2>
            </div>
            <div class="col-sm">
            </div>
        </div>
    </div>
    {#
        {{ form_start(form2) }}
        <div class="container">
            <div class="row">
                <div class="col-sm">
                    <h2>Administrator Informations</h2>
                </div>
                <div class="col-sm">
                </div>
                <div class="col-sm">
                </div>
            </div>
        </div>

        <div class="col-sm-8">{{ form_row(form2.firstName)}}</div>
        <div class="col-sm-8">{{ form_row(form2.lastName)}}</div>
        <div class="col-6 col-md-4">{{ form_row(form2.personalMail)}}</div>


        {{ form_row(form2._token) }}
        {{ form_end(form2) }}
    #}

    {{ form_start(form) }}
    <div class="container">
        <div class="row">
            <div class="col-sm">
                <h2>Administrator Informations</h2>
            </div>
            <div class="col-sm">
            </div>
            <div class="col-sm">
            </div>
        </div>
    </div>
   <div class="col-sm-8">  {{ form_row(form.administrator.firstName) }}</div>
   <div class="col-sm-8"> {{ form_row(form.administrator.lastName) }}</div>
   <div class="col-6 col-md-4"> {{ form_row(form.administrator.personalMail) }}</div>

    <div class="container">
        <div class="row">
            <div class="col-sm">
                <h2>School Informations</h2>
            </div>
            <div class="col-sm">
            </div>
            <div class="col-sm">
            </div>
        </div>
    </div>

    <div class="col-sm-8">{{ form_row(form.name)}}</div>
    <div class="col-sm-8">{{ form_row(form.address)}}</div>
    <div class="col-6 col-md-4">{{ form_row(form.phoneNumber)}}</div>
    <div class="col-6 col-md-4">{{ form_row(form.email)}}</div>

    <div class="col-sm-10">{{ form_row(form.submit)}}</div>
    {{ form_row(form._token) }}
    {{ form_end(form, {'render_rest': false}) }}
{% endblock %}



{% block javascripts %}
    {{ parent () }}
    <script>

    </script>
{% endblock %}