<nav class="navbar navbar-expand-lg navbar-dark">
    <img src="{{ asset('img/base.png') }}" class="img-fluid" alt="Responsive image" style="width: 170px">
    <div class="container">
        <a class="navbar-brand" href="{{ path('welcome') }}"><strong>Home</strong></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('registration') }}">Registr</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('matrix') }}">Matr Of Exp</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('student_page') }}">Student</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_page') }}">User</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('minorBehavior', {'id': 30}) }}">Min& Maj</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('screening_tool', {'id': 291}) }}">ScreenTool</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('cico', {'id': 291}) }}">CICO</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('rewards') }}">Rewards</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('odr') }}">ODR</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('generate_access_data', {'id': 108}) }}">GenData</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_page') }}">Auth0</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_page') }}">UsersRole</a>
                </li>

                {% if app.user %}

                    {% if instanceOf(app.user, 'User') %}
                        {% set logoutPath = 'logout' %}
                    {% else %}
                        {% set logoutPath = 'app_logout' %}
                    {% endif %}

                    <a href="{{ path(logoutPath) }}">
                        <button type="button" class="btn btn-primary btn-sm">Logout</button>
                    </a>
                {% else %}
                    <a href="{{ path("app_login") }}"><button type="button" class="btn btn-primary btn-sm">Login</button></a>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>